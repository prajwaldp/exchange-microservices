{
  "routes": [
    {
      "server": "localhost:8081",
      "filters": "/users",
      "auth_required": true
    },
    {
      "server": "localhost:8082",
      "filters": "/articles",
      "auth_required": false
    }
  ]
}